services:
  - type: web
    name: halal-stock-checker
    runtime: python
    buildCommand: pip install -r requirements-email.txt
    startCommand: gunicorn email_app:app --timeout 120
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: GMAIL_APP_PASSWORD
        sync: false
      - key: SENDER_EMAIL
        sync: false
      - key: SENDER_NAME
        sync: false
  - type: cron
    name: nightly-price-update
    runtime: python
    schedule: "0 2 * * *"
    buildCommand: pip install -r requirements.txt
    startCommand: python jobs/nightly_price_update.py
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: CRON_SECRET
        sync: false
